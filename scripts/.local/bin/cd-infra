#! /usr/bin/env bash

if [ "$#" -ne 1 ]; then
    echo "Specify the environment where you want to jump." >&2
fi

git_root="$(git rev-parse --show-toplevel)"

# shellcheck disable=SC2164
cd "$(pwd | sed -E "s|$git_root/envs/(\w+\b)|$git_root/envs/${1}|")"
